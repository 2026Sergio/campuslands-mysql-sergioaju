# Resolución Ejercicio 001 (Intermedio) - INNER JOIN MOBA

**Estudiante:** Allison Vargas  
**Nivel:** Intermedio Inicial  

## Descripción
Modelado relacional entre las entidades `equipos` y `jugadores`, aplicando restricciones de clave foránea y consultas analíticas mediante `INNER JOIN`.

## Estructura
- `ddl/schema.sql`: Creación de tablas relacionadas y claves foráneas con `ON DELETE CASCADE`.
- `dml/inserts.sql`: Carga de equipos y jugadores asociados.
- `dql/consultas.sql`: 5 consultas con agregaciones, cruce de tablas y filtros por región.
