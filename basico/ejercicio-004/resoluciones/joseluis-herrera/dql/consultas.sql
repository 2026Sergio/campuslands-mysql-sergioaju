SELECT * FROM motos

SELECT * FROM motos 
WHERE cilindrada >  300

SELECT 
	marca,
    modelo,
    pais_origin
    FROM motos 
	WHERE pais_origin = "JAPON"
    
SELECT 
	marca,
    modelo,
    pais_origin
    FROM motos 
	WHERE pais_origin = "India"
    